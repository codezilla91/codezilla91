name: Generate Snake Animation

on:
  # Run automatically every 12 hours
  schedule:
    - cron: "0 */12 * * *"
  # Allow manual triggering from the Actions tab
  workflow_dispatch:
  # Regenerate whenever main is pushed
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 10
    steps:
      - name: Generate snake SVGs
        uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=#318CE7&color_dots=#e1e1e1,#9ec5f0,#5ba1ec,#318CE7,#1a5fa0
            dist/github-snake-dark.svg?palette=github-dark&color_snake=#318CE7
            dist/github-snake.gif?color_snake=#318CE7&color_dots=#e1e1e1,#9ec5f0,#5ba1ec,#318CE7,#1a5fa0

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
